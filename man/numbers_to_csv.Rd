% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numbers_to_csv.R
\name{numbers_to_csv}
\alias{numbers_to_csv}
\title{Export tables from Apple Numbers (.numbers) file to CSV}
\usage{
numbers_to_csv(
  in_path,
  out_dir,
  create_dir = TRUE,
  na_rep = "",
  python_env = NULL,
  verbose = TRUE,
  progress = TRUE
)
}
\arguments{
\item{in_path}{Path to the .numbers file.}

\item{out_dir}{Output directory to write CSV files.}

\item{create_dir}{If TRUE, creates the output directory if it doesn't exist.}

\item{na_rep}{Representation for missing values in output CSVs (default: "").}

\item{python_env}{Path to Python executable or Conda environment name. If NULL, uses default Python.}

\item{verbose}{If TRUE, prints progress and file write messages.}

\item{progress}{If TRUE and `progress` package is available, shows a progress bar.}
}
\value{
Invisibly returns a character vector of written file paths.
}
\description{
This function reads an Apple Numbers (.numbers) file, extracts tables from each sheet,
and exports them as CSV files, mirroring the Python script using `numbers_parser`.
}
\examples{
\dontrun{
  numbers_to_csv(
    in_path = "example.numbers",
    out_dir = "output",
    create_dir = TRUE,
    na_rep = "",
    python_env = NULL,
    verbose = TRUE,
    progress = TRUE
  )
}

}
